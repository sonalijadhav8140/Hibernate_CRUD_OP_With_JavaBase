# Hibernate CRUD Project with Java-based Configuration

## 📌 Project Overview
This project demonstrates Hibernate CRUD (Create, Read, Update, Delete) operations using Java-based configuration instead of `hibernate.cfg.xml`. It follows a fully Java-based approach for setting up Hibernate.

## 🛠️ Technologies Used
- Java
- Hibernate (ORM Framework)
- MySQL 
